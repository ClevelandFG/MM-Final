# AI Agent 指令集

## 1. 项目定位

- **目标**：基于 `docs/theories.md` 实现`docs/task.md`所要求的研究任务，以及可能的拓展任务 。
- **范围**：当前优先完成路线优化建模、可复现实验、结果可视化与报告支撑；GUI 属于展示与交互层，不承载核心数学逻辑。
- **协作**：两人协作，使用 Git 管理全部项目文件；远程 public 仓库为 `https://github.com/ClevelandFG/MM-Final`。
- **Git 工作流**：A/B 双线采用短生命周期功能分支；共享契约和公共数据结构必须单独使用 `shared/...` 分支优先合并。具体规则见 `docs/git-workflow.md`。
- **交付分工**：GUI 与可视化由 B 线工程师负责；上台展示由 A 线工程师负责；书面报告由两人共同完成，各自负责自己工作内容对应的报告部分。

## 2. 技术与架构原则

- **语言**：已确认尽可能只使用 Python；保留后期在确有性能瓶颈或工程必要性时引入 C++ 加速核心的可能。
- **依赖配置**：采用 `pyproject.toml` 作为项目元数据、依赖声明和工具配置入口；采用 uv 管理虚拟环境同步和锁文件；提交 `pyproject.toml`、`uv.lock` 与环境说明，不提交 `.venv`。依赖和环境策略见 `docs/environment-and-dependencies.md`。
- **开发方式**：采用 TDD，先写测试再实现，便于快速定位问题。
- **范式**：严格 OOP，优先抽象优化方法、IO、计时等通用接口。
- **前后端边界**：后端负责数学状态、参数校验、方法执行、IO 与计时；GUI 只负责交互和展示。模糊归属必须先列出“前端实现 vs 后端实现”的利弊，由用户决策。
- **计时系统**：支持分步骤计时，用于定位性能瓶颈。
- **实现尺度**：只写最小正确实现，避免过早膨胀。
- **原始数据**：题面道路表固化到 `data/raw/road_network.tsv`；后续算法与测试统一从该文件读取，避免复制多份路网数据。
- **统一路网标准**：B 线在 `shared/road-network-core` 中确立的路网读取、节点分类、边权校验、连通性校验和 `mm_final.network` 公共接口，是 A/B 两线最终统一标准。A 线开工前必须先阅读 `docs/detailed-plan-for-track-A.md`、`docs/detailed-plan-for-track-B.md` 的 B1 公共底基决策，并基于最新 `main` 使用 `mm_final.network`，不得另起一套路网语义。
- **A/B 对接协议**：A 线负责路线构造，B 线负责耗时审计、下界证明与参数分析；两线必须统一使用 `docs/contracts/route-plan-contract.md` 定义的路线方案格式。任何算法输出或审计输入不得绕过该契约。

## 3. 文档与代码规范

- **交流语言**：全程中文。
- **需求澄清**：沟通需求、方案、边界条件和架构决策时，必须使用 `grill-with-docs` 技能；能从代码或文档确认的内容先自行查证。
- **决策沉淀**：术语和边界共识及时写入 `docs/context.md`；改动记录写入 `docs/changes.md`；重大且难逆转的权衡再写 ADR。
- **文档读取**：读取中文文档时必须显式使用 UTF-8 编码，避免控制台默认编码导致中文乱码。
- **注释**：需要使用中文工程型注释说明模块意图；改动历史不写进代码注释。
- **命名与结构**：变量命名意图导向，目录保持工程级整洁。
- **版本号**：采用 `x.y.z`；第一个可行版本前保持 `x=0`。
- **Agent 文件**：项目内 `.agents/` 与 `.codex/` 目录可用于保存不含敏感信息的协作记录，并纳入 Git 管理。
- **契约变更**：路线方案字段、单位、节点语义或审计口径发生变化时，必须同步更新契约文档、测试和 `docs/changes.md`。
- **共享分支优先**：涉及 `RoutePlan` 等公共结构、测试夹具或路线方案契约的变更，不得夹带在 A 线或 B 线功能分支中；必须先合并到 `main`，再让两线基于最新主干继续开发。
- **Git 操作建议**：项目分工较复杂，智能体每次关键操作前后都必须向工程师建议 Git 操作，包括应在哪个分支修改、是否需要先同步 `main`、是否应提交、是否应推送、是否应先走 `shared/...` 分支。

## 4. 交互协议

- 遇到模糊需求时不得直接写代码，必须先提出具体问题，并尽量提供选项、分析与建议。
- 搭建与实现按“先讨论计划、达成共识、再分阶段实施”的流程推进。
