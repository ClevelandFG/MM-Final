# AI Agent 指令集

## 1. 项目定位

- **目标**：基于 `docs/theories.md` 实现`docs/task.md`所要求的研究任务，以及可能的拓展任务 。
- **范围**：当前优先完成路线优化建模、可复现实验、结果可视化与报告支撑；GUI 属于展示与交互层，不承载核心数学逻辑。
- **协作**：两人协作，使用 Git 管理全部项目文件；远程 public 仓库为 `https://github.com/ClevelandFG/MM-Final`。

## 2. 技术与架构原则

- **语言**：尚未最终确定，候选为 Python 或 C++；确定前保持目录与文档语言无关，避免提前绑定工具链。
- **开发方式**：采用 TDD，先写测试再实现，便于快速定位问题。
- **范式**：严格 OOP，优先抽象优化方法、IO、计时等通用接口。
- **前后端边界**：后端负责数学状态、参数校验、方法执行、IO 与计时；GUI 只负责交互和展示。模糊归属必须先列出“前端实现 vs 后端实现”的利弊，由用户决策。
- **计时系统**：支持分步骤计时，用于定位性能瓶颈。
- **实现尺度**：只写最小正确实现，避免过早膨胀。
- **原始数据**：题面道路表固化到 `data/raw/road_network.tsv`；后续算法与测试统一从该文件读取，避免复制多份路网数据。

## 3. 文档与代码规范

- **交流语言**：全程中文。
- **需求澄清**：沟通需求、方案、边界条件和架构决策时，必须使用 `grill-with-docs` 技能；能从代码或文档确认的内容先自行查证。
- **决策沉淀**：术语和边界共识及时写入 `docs/context.md`；改动记录写入 `docs/changes.md`；重大且难逆转的权衡再写 ADR。
- **文档读取**：读取中文文档时必须显式使用 UTF-8 编码，避免控制台默认编码导致中文乱码。
- **注释**：需要使用中文工程型注释说明模块意图；改动历史不写进代码注释。
- **命名与结构**：变量命名意图导向，目录保持工程级整洁。
- **版本号**：采用 `x.y.z`；第一个可行版本前保持 `x=0`。
- **Agent 文件**：项目内 `.agents/` 与 `.codex/` 目录可用于保存不含敏感信息的协作记录，并纳入 Git 管理。

## 4. 交互协议

- 遇到模糊需求时不得直接写代码，必须先提出具体问题，并尽量提供选项、分析与建议。
- 搭建与实现按“先讨论计划、达成共识、再分阶段实施”的流程推进。
